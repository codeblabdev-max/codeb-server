# CodeB Platform Architecture
> Coolify/Vercel 스타일 CLI 기반 프로젝트 관리 시스템

## 📋 목차
1. [시스템 개요](#시스템-개요)
2. [아키텍처 설계](#아키텍처-설계)
3. [배포 플로우](#배포-플로우)
4. [표준 프로젝트 구조](#표준-프로젝트-구조)
5. [CLI 명령어 체계](#cli-명령어-체계)
6. [서버 구성 요소](#서버-구성-요소)

---

## 🎯 시스템 개요

### 핵심 개념
```
개발자 로컬 환경 (codeb CLI 전역 설치)
    ↓ (배포 명령)
141 서버 (CodeB API + PM2 + Podman)
    ↓ (자동 프로비저닝)
프로젝트별 독립 환경 (App + DB + Redis + Env)
```

### 목표
- ✅ **표준화**: 모든 프로젝트가 동일한 패턴으로 배포
- ✅ **격리**: 프로젝트별 독립된 DB/Redis/Env
- ✅ **자동화**: 수동 설정 최소화
- ✅ **확장성**: 포트 자동 할당, 리소스 관리
- ✅ **편의성**: CLI 하나로 모든 작업 완료

---

## 🏗️ 아키텍처 설계

### Layer 1: 개발자 환경 (로컬)
```bash
┌─────────────────────────────────────┐
│ 개발자 PC                            │
│                                      │
│ $ npm install -g codeb-cli          │
│ $ codeb login                       │
│ $ codeb create my-app --db postgres│
│ $ codeb deploy my-app               │
│ $ codeb env:set KEY=value           │
│ $ codeb logs my-app                 │
└─────────────────────────────────────┘
```

**설치 방식**:
- SuperClaude처럼 전역 설치 (`npm i -g codeb-cli`)
- 인증: SSH key 또는 API token
- 설정 파일: `~/.codeb/config.json`

### Layer 2: 141 서버 인프라

```
┌──────────────────────────────────────────────────────────┐
│ 141.164.60.51 (CodeB 서버)                               │
│                                                           │
│ ┌─────────────────────────────────────────────────────┐  │
│ │ CodeB API Server (포트 3020)                        │  │
│ │ - 프로젝트 등록/관리                                 │  │
│ │ - 포트 할당 (자동 충돌 방지)                         │  │
│ │ - 배포 오케스트레이션                                │  │
│ │ - 환경변수 관리                                      │  │
│ │ - PowerDNS 도메인 자동 설정                          │  │
│ └─────────────────────────────────────────────────────┘  │
│                                                           │
│ ┌─────────────────────────────────────────────────────┐  │
│ │ PowerDNS (네임서버)                                  │  │
│ │ - 자동 도메인 생성: project-name.your-domain.com    │  │
│ │ - A 레코드 자동 등록                                 │  │
│ └─────────────────────────────────────────────────────┘  │
│                                                           │
│ ┌──────────────┬──────────────┬──────────────┐          │
│ │ Project A    │ Project B    │ Project C    │          │
│ ├──────────────┼──────────────┼──────────────┤          │
│ │ PM2: app     │ PM2: app     │ PM2: app     │          │
│ │ Port: 3100   │ Port: 3101   │ Port: 3102   │          │
│ │              │              │              │          │
│ │ 🐳 Postgres  │ 🐳 Postgres  │ 🐳 MySQL     │          │
│ │ Port: 5432   │ Port: 5433   │ Port: 3306   │          │
│ │              │              │              │          │
│ │ 🐳 Redis     │ 🐳 Redis     │ 🐳 Redis     │          │
│ │ Port: 6379   │ Port: 6380   │ Port: 6381   │          │
│ │              │              │              │          │
│ │ .env (격리)  │ .env (격리)  │ .env (격리)  │          │
│ └──────────────┴──────────────┴──────────────┘          │
└──────────────────────────────────────────────────────────┘
```

### Layer 3: 프로젝트 격리 환경

각 프로젝트는 **완전히 독립된 환경**:

```
/opt/projects/my-app/
├── app/                          # PM2로 실행되는 Node.js 앱
│   ├── .env                      # 프로젝트별 환경변수
│   ├── package.json
│   └── ...
├── .codeb/                       # CodeB 메타데이터
│   ├── project.json              # 프로젝트 설정
│   ├── containers.json           # 컨테이너 정보
│   └── ports.json                # 할당된 포트
└── docker-compose.yml            # Podman DB/Redis 정의
```

---

## 🚀 배포 플로우

### 1. 프로젝트 생성 (최초 1회)
```bash
개발자: $ codeb create my-app --db postgres --cache redis
```

**자동 실행**:
1. ✅ 141 서버에 프로젝트 등록
2. ✅ 포트 자동 할당 (app: 3100, db: 5432, redis: 6379)
3. ✅ Podman으로 DB/Redis 컨테이너 생성
4. ✅ 프로젝트 디렉토리 생성 (`/opt/projects/my-app`)
5. ✅ .env 파일 자동 생성 (DB 연결 정보 포함)
6. ✅ PowerDNS에 도메인 등록 (`my-app.your-domain.com`)

### 2. 코드 배포
```bash
개발자: $ codeb deploy my-app
```

**자동 실행**:
1. ✅ Git에서 코드 pull (또는 로컬 코드 rsync)
2. ✅ npm install
3. ✅ npm run build
4. ✅ PM2로 앱 시작/재시작
5. ✅ Health check (3회 재시도)
6. ✅ 성공 시 도메인 활성화

### 3. 환경변수 관리
```bash
개발자: $ codeb env:set my-app KEY=value
개발자: $ codeb env:list my-app
```

**보안**:
- 환경변수는 서버에서만 저장
- 로컬에서는 암호화된 형태로 전송
- .env 파일 직접 수정 금지

### 4. 데이터베이스 관리
```bash
개발자: $ codeb db:add my-app postgres    # 추가 DB 생성
개발자: $ codeb db:backup my-app          # 백업
개발자: $ codeb db:restore my-app backup.sql
```

### 5. 모니터링
```bash
개발자: $ codeb status my-app
개발자: $ codeb logs my-app -f
개발자: $ codeb ps                        # 모든 프로젝트 목록
```

---

## 📁 표준 프로젝트 구조

### 서버 디렉토리 구조
```
/opt/
├── codeb-api/                    # CodeB API 서버
│   ├── api-server.js
│   ├── routes/
│   └── lib/
├── projects/                     # 모든 프로젝트
│   ├── my-app/
│   │   ├── app/                  # 실제 앱 코드
│   │   ├── .codeb/               # 메타데이터
│   │   └── docker-compose.yml    # DB/Redis
│   ├── starpick/
│   ├── warehouse-rental/
│   └── ...
└── codeb-backups/                # 전체 백업
```

### 프로젝트별 `.codeb/project.json`
```json
{
  "name": "my-app",
  "version": "1.0.0",
  "created": "2025-10-29T00:00:00Z",
  "ports": {
    "app": 3100,
    "postgres": 5432,
    "redis": 6379
  },
  "containers": {
    "postgres": "my-app-postgres",
    "redis": "my-app-redis"
  },
  "pm2": {
    "name": "my-app",
    "instances": 1,
    "exec_mode": "fork"
  },
  "domain": "my-app.your-domain.com",
  "env": {
    "NODE_ENV": "production",
    "PORT": "3100",
    "DATABASE_URL": "postgresql://user:pass@localhost:5432/my-app"
  }
}
```

---

## 💻 CLI 명령어 체계

### 프로젝트 관리
```bash
codeb create <name> [options]      # 프로젝트 생성
  --db postgres|mysql              # DB 타입
  --cache redis|memcached          # 캐시 타입
  --template nextjs|remix|node     # 템플릿

codeb deploy <name>                # 배포
codeb delete <name>                # 삭제
codeb list                         # 전체 목록
codeb status <name>                # 상태 확인
```

### 환경 관리
```bash
codeb env:set <name> KEY=VALUE     # 환경변수 설정
codeb env:get <name> KEY           # 환경변수 조회
codeb env:list <name>              # 전체 환경변수
codeb env:delete <name> KEY        # 환경변수 삭제
```

### 데이터베이스
```bash
codeb db:add <name> <type>         # DB 추가
codeb db:backup <name>             # 백업
codeb db:restore <name> <file>     # 복원
codeb db:shell <name>              # DB 접속
```

### 제어
```bash
codeb start <name>                 # 시작
codeb stop <name>                  # 중지
codeb restart <name>               # 재시작
codeb logs <name> [-f]             # 로그 확인
```

### 설정
```bash
codeb login                        # 서버 인증
codeb config:set server 141.164.60.51
codeb config:get server
```

---

## 🔧 서버 구성 요소

### 1. CodeB API Server (포트 3020)

**역할**:
- 프로젝트 CRUD
- 포트 할당 및 충돌 방지
- Podman 오케스트레이션
- PM2 관리
- PowerDNS 연동

**엔드포인트**:
```
POST   /projects                  # 프로젝트 생성
GET    /projects                  # 목록 조회
GET    /projects/:name            # 상세 조회
DELETE /projects/:name            # 삭제

POST   /projects/:name/deploy     # 배포
POST   /projects/:name/env        # 환경변수 설정
GET    /projects/:name/logs       # 로그 조회

POST   /ports/allocate            # 포트 할당
GET    /ports/available           # 사용 가능 포트
```

### 2. 포트 관리 시스템

**포트 범위**:
```javascript
{
  app: { start: 3000, max: 1000 },      // 3000-3999
  postgres: { start: 5432, max: 100 },  // 5432-5531
  mysql: { start: 3306, max: 100 },     // 3306-3405
  redis: { start: 6379, max: 100 }      // 6379-6478
}
```

**충돌 방지**:
- 프로젝트 생성 시 사용 중인 포트 스캔
- 다음 사용 가능한 포트 자동 할당
- `ports.json`에 기록하여 영구 저장

### 3. PowerDNS 연동

**자동 도메인 생성**:
```bash
프로젝트명: my-app
→ 도메인: my-app.your-domain.com
→ A 레코드: 141.164.60.51
```

**API 호출**:
```javascript
await pdnsAPI.createRecord({
  zone: 'your-domain.com',
  name: 'my-app',
  type: 'A',
  content: '141.164.60.51'
});
```

---

## 🔐 보안 고려사항

1. **인증**: SSH key 기반 또는 API token
2. **환경변수**: 암호화 전송 및 저장
3. **DB 비밀번호**: 자동 생성 (32자 랜덤)
4. **포트 노출**: 방화벽 규칙 자동 설정
5. **로그**: 민감 정보 필터링

---

## 📊 현재 상황 vs 목표

### 현재
- ❌ 프로젝트별 포트 충돌
- ❌ 수동 DB 생성
- ❌ 환경변수 수동 관리
- ❌ 배포 프로세스 일관성 부족
- ✅ PM2 사용
- ✅ Podman 인프라 존재
- ✅ CLI 기본 구조 존재

### 목표
- ✅ 자동 포트 할당
- ✅ 자동 DB/Redis 프로비저닝
- ✅ CLI로 환경변수 관리
- ✅ 표준화된 배포 플로우
- ✅ 전역 설치 CLI
- ✅ PowerDNS 자동 설정

---

## 🗺️ 마이그레이션 계획

### Phase 1: 인프라 정비
1. CodeB API 서버 완성
2. 포트 관리 시스템 구현
3. PowerDNS 연동

### Phase 2: CLI 개발
1. 전역 설치 가능한 npm 패키지
2. 핵심 명령어 구현 (create, deploy, env)
3. 인증 시스템

### Phase 3: 기존 프로젝트 마이그레이션
1. vsvs → CodeB 표준으로 전환
2. warehouse-rental → 문제 해결 후 표준화
3. starpick, misopin-cms 등 순차 마이그레이션

### Phase 4: 배포 및 운영
1. 직원들에게 CLI 배포
2. 표준 매뉴얼 작성
3. 모니터링 및 최적화

---

## 🎓 사용 예시

### 신규 프로젝트 배포
```bash
# 1. 프로젝트 생성
$ codeb create my-blog --db postgres --cache redis --template nextjs

# 2. 코드 작성 (로컬)
$ cd my-blog
$ npm run dev

# 3. 배포
$ codeb deploy my-blog

# 4. 환경변수 설정
$ codeb env:set my-blog STRIPE_KEY=sk_test_xxx

# 5. 확인
$ codeb status my-blog
$ codeb logs my-blog -f

# 6. 접속
$ open https://my-blog.your-domain.com
```

---

## 📝 다음 단계

1. ✅ 아키텍처 설계 완료 (현재 문서)
2. ⏭️ CodeB API 서버 구현
3. ⏭️ CLI v4.0 개발
4. ⏭️ 테스트 배포
5. ⏭️ 문서화 및 배포
