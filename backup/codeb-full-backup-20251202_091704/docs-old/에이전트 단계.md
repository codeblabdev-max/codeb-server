 MCP와 Claude Code Sub-Agent를 활용한 대응 방법을 제안드리겠습니다.

  📋 문제 해결 전략

⏺ 🎯 대응 방법

  1. MCP 서버 활용법

⏺ 현재 활용 가능한 MCP 서버들:

  - sequential-thinking: 복잡한 문제 단계별 분석
  - context7: 라이브러리 문서 검색
  - memory: 지식 그래프 구축
  - firecrawl: 웹 리소스 수집
  - shrimp-task-manager: 작업 관리 및 분석

  2. Sub-Agent 델리게이션 전략

  대규모 분석 작업:
    명령: /analyze @project --delegate folders --concurrency 7
    효과:
      - 디렉토리별 병렬 분석
      - 70% 시간 단축
      - 컨텍스트 분산

  중복 의존성 정리:
    명령: /cleanup --delegate files --focus dependencies
    작업 분할:
      - package.json 분석 (Agent 1)
      - import 문 스캔 (Agent 2)
      - 사용 패턴 분석 (Agent 3)

  리팩토링 작업:
    명령: /improve --wave-mode --systematic
    Wave 전략:
      1. Review Wave: 현재 코드 분석
      2. Planning Wave: 개선 계획 수립
      3. Implementation Wave: 순차적 적용
      4. Validation Wave: 검증 및 테스트

  3. 컨텍스트 관리 기법

  # 1. 초기 프로젝트 로드 (컨텍스트 최적화)
  /load @project --uc --delegate auto

  # 2. 세밀한 분석 (MCP 활용)
  /analyze --seq --think-hard --focus architecture

  # 3. 중복 제거 작업
  /cleanup --persona-refactorer --validate

  # 4. Socket.io 최적화
  /improve @websocket --focus performance --wave-mode

  4. 실전 활용 예시

  Step 1: 프로젝트 구조 파악
  # shrimp-task-manager로 분석 계획 수립
  /task analyze --seq --delegate folders

  Step 2: 중복 의존성 제거
  # 병렬 처리로 빠른 스캔
  /spawn cleanup-deps --parallel-focus
    - Frontend 의존성 (React/Next.js)
    - Backend 의존성 (Socket.io)
    - DB 관련 (PostgreSQL)

  Step 3: 구조 정리
  # Wave 모드로 점진적 개선
  /improve --wave-mode --wave-strategy progressive

  5. MCP 서버 연계 활용

  // 예: shrimp-task-manager + sequential 조합
  작업 계획: shrimp-task-manager로 수립
  → 복잡한 로직: sequential-thinking으로 분석
  → 문서 참조: context7로 best practice 확인
  → 결과 저장: memory로 지식 그래프 구축

  6. Podman 환경 최적화

  # 컨테이너 구조 분석
  /analyze @containers --delegate --focus infrastructure

  # 마이크로서비스 분리
  /design microservices --persona-architect --seq

  추천 워크플로우:

  1. 분석 단계: --delegate folders + --seq 조합
  2. 정리 단계: --wave-mode + --persona-refactorer
  3. 구현 단계: --parallel-focus + MCP 서버들
  4. 검증 단계: --validate + --playwright

  구체적으로 어떤 부분부터 시작하시겠습니까? 프로젝트 구조를 먼저 보여주시면 맞춤형 전략을 세울 수 있습니다.