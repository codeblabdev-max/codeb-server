

수동으로 환경 변수를 확인하는 중...
# CodeB Platform - Remote Environment (Manual)
# Project: starpick-platform
# Generated: Fri Sep  5 05:27:59 KST 2025

# Database (PostgreSQL) - 컨테이너에서 확인
DATABASE_URL="postgresql://user_starpick-platform:PASSWORD@141.164.60.51:PORT/db_starpick-platform"
POSTGRES_HOST="141.164.60.51"
POSTGRES_PORT="5434"  # 실제 포트 확인 필요
POSTGRES_USER="user_starpick-platform"
POSTGRES_PASSWORD="PASSWORD"  # 컨테이너 환경변수 확인 필요
POSTGRES_DB="db_starpick-platform"

# Redis - 아직 생성되지 않음
REDIS_URL="redis://:PASSWORD@141.164.60.51:6381/0"
REDIS_HOST="141.164.60.51"
REDIS_PORT="6381"

# API
NEXT_PUBLIC_API_URL="http://141.164.60.51:3008/api"
API_BASE_URL="http://141.164.60.51:3008"

# Environment
NODE_ENV="development"
PROJECT_NAME="starpick-platform"

[1;33m📌 실제 값을 확인하려면:[0m
  ssh root@141.164.60.51 'podman inspect codeb-db-starpick-platform | jq ".[].Config.Env"'



  env init <local|server>     환경 초기화
  env switch <local|server>   환경 전환
  env status                  현재 환경 상태

  local start                 Podman 컨테이너 시작 (DB/Redis)
  local stop                  Podman 컨테이너 중지
  local reset                 컨테이너 초기화
  local status                컨테이너 상태 확인

    --with-db=<postgresql|mysql>              DB 설정
    --with-cache=<redis|memcached>            캐시 설정
    --with-storage=<local|s3|gcs>             스토리지 설정



  build <이름> [build|dev|start]    빌드 실행

  db migrate <이름>           마이그레이션 실행
  db seed <이름>              시드 데이터 생성

  logs <이름> [app|build|pm2]  로그 보기
  diagnose <이름>             종합 진단


