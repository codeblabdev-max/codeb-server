# ============================================================================
# CodeB /we: CLI Configuration
# 이 파일을 ~/.codeb/config.json 또는 프로젝트 .env에 설정하세요
#
# 설정 우선순위:
#   1. 환경 변수 (export CODEB_SERVER_HOST=...)
#   2. ~/.codeb/config.json
#   3. 프로젝트 .env 파일
# ============================================================================

# ==================== 필수 설정 ====================

# 배포 서버 IP 주소 (필수)
# 관리자로부터 받은 서버 IP를 입력하세요
CODEB_SERVER_HOST=

# 서버 SSH 사용자명 (기본값: root)
CODEB_SERVER_USER=root

# ==================== 데이터베이스 설정 ====================

# PostgreSQL 비밀번호
# 각 프로젝트별로 별도 설정 가능
CODEB_DB_PASSWORD=

# ==================== 도메인 설정 ====================

# 기본 도메인 (예: codeb.kr, codeb.dev)
CODEB_DOMAIN=codeb.kr

# ==================== 선택 설정 (API Keys) ====================

# Vultr API Key (SSH 키 관리용)
# 발급: https://my.vultr.com/settings/#settingsapi
# 주의: SSH Key 관리만 가능, 서버 생성/삭제 권한 없음
VULTR_API_KEY=

# GitHub Personal Access Token
# 발급: https://github.com/settings/tokens
# 권한: repo, workflow, write:packages
GITHUB_TOKEN=

# ============================================================================
# 설정 방법
# ============================================================================
#
# 방법 1: 환경 변수로 설정 (세션용)
#   export CODEB_SERVER_HOST=141.164.60.51
#   export CODEB_DB_PASSWORD=your_password
#
# 방법 2: ~/.codeb/config.json 생성 (영구 설정)
#   we config init
#
# 방법 3: 프로젝트 .env 파일 생성
#   cp .env.example .env
#   # 값 입력 후 저장
#
# ============================================================================
