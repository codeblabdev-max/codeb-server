# CodeB v6.0 명령어 및 서비스 인벤토리

> 분석일: 2026-01-07
> 목적: 실제 사용되는 기능 식별 및 불필요한 기능 정리

---

## 1. 현재 시스템 구조

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                        CodeB v6.0 시스템 구조                                │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌─────────────────┐    ┌─────────────────┐    ┌─────────────────┐         │
│  │  Claude Code    │    │   we CLI        │    │  GitHub Actions │         │
│  │  (AI Agent)     │    │   (Developer)   │    │  (CI/CD)        │         │
│  └────────┬────────┘    └────────┬────────┘    └────────┬────────┘         │
│           │                      │                      │                   │
│           ▼                      ▼                      ▼                   │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                    Slash Commands (Skills)                          │   │
│  │  /we:deploy, /we:workflow, /we:health, /we:domain, etc.            │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                         MCP Server                                   │   │
│  │                   codeb-deploy (SSH 기반)                           │   │
│  │              → 현재 .mcp.json에 설정됨                               │   │
│  │              → 파일 없음 (codeb-deploy-system 삭제됨)                 │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                   v6.0 HTTP API Server                              │   │
│  │                  (Express + TypeScript)                             │   │
│  │                    45개 Tool 등록                                   │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                    │                                        │
│                                    ▼                                        │
│  ┌─────────────────────────────────────────────────────────────────────┐   │
│  │                      Server Infrastructure                          │   │
│  │            n1 (App) / n2 (Streaming) / n3 (Storage) / n4 (Backup)  │   │
│  └─────────────────────────────────────────────────────────────────────┘   │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 2. Slash Commands 인벤토리

### 2.1 프로젝트 레벨 (/we:*)

| 명령어 | 위치 | 실사용 | 상태 | 비고 |
|--------|------|:------:|:----:|------|
| `/we:deploy` | project | ✅ | 🟢 유지 | 핵심 배포 명령 |
| `/we:workflow` | project | ✅ | 🟢 유지 | CI/CD 생성 |
| `/we:health` | project | ✅ | 🟢 유지 | 서버 상태 확인 |
| `/we:domain` | project | ✅ | 🟢 유지 | 도메인 관리 |
| `/we:rollback` | project | ✅ | 🟢 유지 | 롤백 |
| `/we:monitor` | project | ⚠️ | 🟡 검토 | 실사용 빈도 낮음 |
| `/we:agent` | project | ⚠️ | 🟡 검토 | Task tool과 중복 |
| `/we:analyze` | project | ⚠️ | 🟡 검토 | cb-analyze와 중복 |
| `/we:ssh` | project | ⚠️ | 🔴 제거 | Admin 직접 사용만 |
| `/we:optimize` | project | ⚠️ | 🟡 검토 | cb-optimize와 중복 |

### 2.2 글로벌 유저 레벨 (/we:*)

| 명령어 | 위치 | 실사용 | 상태 | 비고 |
|--------|------|:------:|:----:|------|
| `/we:init` | user | ✅ | 🟢 유지 | 프로젝트 초기화 |
| `/we:secrets` | user | ✅ | 🟢 유지 | GitHub Secrets |
| `/we:registry` | user | ✅ | 🟢 유지 | 레지스트리 관리 |
| `/we:test` | user | ⚠️ | 🟡 검토 | Playwright |

### 2.3 CodeB 에이전트 (/cb*) - ⚠️ 레거시 (제거 대상)

> **중요**: `/cb*` 명령어는 이전 MCP 시스템의 레거시 코드입니다. 모두 제거 대상입니다.

| 명령어 | 위치 | 상태 | 비고 |
|--------|------|:----:|------|
| `/cb` | project | 🔴 제거 | 레거시 MCP |
| `/cb-analyze` | project | 🔴 제거 | 레거시 MCP |
| `/cb-optimize` | project | 🔴 제거 | 레거시 MCP |
| `/cb-cleanup` | project | 🔴 제거 | 레거시 MCP |
| `/cb-pattern` | project | 🔴 제거 | 레거시 MCP |
| `/cb-help` | project | 🔴 제거 | 레거시 MCP |
| `/cb-rollback` | user | 🔴 제거 | 레거시 MCP, we:rollback 사용 |

### 2.4 SC 명령어 (/sc:*) - 외부 MCP (CodeB와 무관)

> **중요**: `/sc:*` 명령어는 별도의 외부 MCP 시스템입니다. CodeB 프로젝트와 무관하므로 여기서 관리하지 않습니다.

| 명령어 | 소속 | 비고 |
|--------|------|------|
| `/sc:implement` | 외부 MCP | 기능 구현 |
| `/sc:analyze` | 외부 MCP | 분석 |
| `/sc:build` | 외부 MCP | 빌드 |
| `/sc:test` | 외부 MCP | 테스트 |
| `/sc:git` | 외부 MCP | Git 작업 |
| `/sc:task` | 외부 MCP | Task 관리 |
| `/sc:workflow` | 외부 MCP | 워크플로우 |
| `/sc:design` | 외부 MCP | 설계 |
| `/sc:document` | 외부 MCP | 문서화 |
| `/sc:estimate` | 외부 MCP | 추정 |
| `/sc:explain` | 외부 MCP | 설명 |
| `/sc:improve` | 외부 MCP | 개선 |
| `/sc:troubleshoot` | 외부 MCP | 디버깅 |
| `/sc:cleanup` | 외부 MCP | 정리 |
| `/sc:spawn` | 외부 MCP | 분기 |
| `/sc:load` | 외부 MCP | 로드 |
| `/sc:index` | 외부 MCP | 인덱스 |

---

## 3. v6.0 HTTP API Tools 인벤토리

### 3.1 팀 관리 (11개)

| Tool | 실사용 | 상태 | 비고 |
|------|:------:|:----:|------|
| `team_create` | ⚠️ | 🟡 | 초기 1회 사용 |
| `team_list` | ⚠️ | 🟡 | 드물게 사용 |
| `team_get` | ⚠️ | 🟡 | 드물게 사용 |
| `team_delete` | ❌ | 🔴 제거 검토 | 거의 미사용 |
| `team_settings` | ⚠️ | 🟡 | 드물게 사용 |
| `member_invite` | ⚠️ | 🟡 | 팀 구성 시만 |
| `member_remove` | ❌ | 🔴 제거 검토 | 거의 미사용 |
| `member_list` | ⚠️ | 🟡 | 드물게 사용 |
| `token_create` | ✅ | 🟢 유지 | 필수 |
| `token_revoke` | ⚠️ | 🟡 | 드물게 사용 |
| `token_list` | ⚠️ | 🟡 | 드물게 사용 |

**권장**: 팀 관리는 초기 설정 후 거의 사용하지 않음. 11개 → 5개로 축소 가능

### 3.2 Blue-Green 배포 (8개)

| Tool | 실사용 | 상태 | 비고 |
|------|:------:|:----:|------|
| `deploy` | ✅ | 🟢 유지 | 핵심 |
| `deploy_project` | ⚠️ | 🔴 제거 | deploy와 중복 |
| `promote` | ✅ | 🟢 유지 | 핵심 |
| `slot_promote` | ⚠️ | 🔴 제거 | promote와 중복 |
| `rollback` | ✅ | 🟢 유지 | 핵심 |
| `slot_status` | ✅ | 🟢 유지 | 상태 확인 |
| `slot_cleanup` | ⚠️ | 🟡 | 자동화 가능 |
| `slot_list` | ⚠️ | 🟡 | slot_status로 대체 가능 |

**권장**: 8개 → 4개 (deploy, promote, rollback, slot_status)

### 3.3 Edge Functions (6개)

| Tool | 실사용 | 상태 | 비고 |
|------|:------:|:----:|------|
| `edge_deploy` | ❌ | 🔴 미구현 | Deno 연동 필요 |
| `edge_list` | ❌ | 🔴 미구현 | 구조만 존재 |
| `edge_logs` | ❌ | 🔴 미구현 | 구조만 존재 |
| `edge_delete` | ❌ | 🔴 미구현 | 구조만 존재 |
| `edge_invoke` | ❌ | 🔴 미구현 | 구조만 존재 |
| `edge_metrics` | ❌ | 🔴 미구현 | 구조만 존재 |

**권장**: 전체 6개 → 0개 (완전 구현 전까지 제거)

### 3.4 Analytics (5개)

| Tool | 실사용 | 상태 | 비고 |
|------|:------:|:----:|------|
| `analytics_overview` | ❌ | 🔴 미구현 | 구조만 존재 |
| `analytics_webvitals` | ❌ | 🔴 미구현 | SDK 필요 |
| `analytics_deployments` | ❌ | 🔴 미구현 | 구조만 존재 |
| `analytics_realtime` | ❌ | 🔴 미구현 | 구조만 존재 |
| `analytics_speed_insights` | ❌ | 🔴 미구현 | 구조만 존재 |

**권장**: 전체 5개 → 0개 (완전 구현 전까지 제거)

### 3.5 Migration (7개)

| Tool | 실사용 | 상태 | 비고 |
|------|:------:|:----:|------|
| `migrate_detect` | ⚠️ | 🟡 | v5→v6 마이그레이션 |
| `migrate_plan` | ⚠️ | 🟡 | 1회성 |
| `migrate_execute` | ⚠️ | 🟡 | 1회성 |
| `migrate_rollback` | ⚠️ | 🟡 | 1회성 |
| `migrate_safe` | ⚠️ | 🟡 | 1회성 |
| `migrate_safe_rollback` | ⚠️ | 🟡 | 1회성 |
| `migrate_generate_workflow` | ⚠️ | 🟡 | 1회성 |

**권장**: 마이그레이션 완료 후 7개 → 0개 제거

### 3.6 ENV 관리 (4개)

| Tool | 실사용 | 상태 | 비고 |
|------|:------:|:----:|------|
| `env_migrate` | ⚠️ | 🟡 | 1회성 |
| `env_scan` | ✅ | 🟢 유지 | 유용함 |
| `env_restore` | ✅ | 🟢 유지 | 필수 |
| `env_backup_list` | ⚠️ | 🟡 | 드물게 사용 |

**권장**: 4개 → 2개 (env_scan, env_restore)

### 3.7 Domain 관리 (5개)

| Tool | 실사용 | 상태 | 비고 |
|------|:------:|:----:|------|
| `domain_setup` | ✅ | 🟢 유지 | 필수 |
| `domain_verify` | ⚠️ | 🟡 | domain_setup에 통합 가능 |
| `domain_list` | ✅ | 🟢 유지 | 유용함 |
| `domain_delete` | ⚠️ | 🟡 | 드물게 사용 |
| `ssl_status` | ⚠️ | 🟡 | domain_list에 통합 가능 |

**권장**: 5개 → 3개 (domain_setup, domain_list, domain_delete)

---

## 4. MCP 서버 상태

### 4.1 현재 설정 (.mcp.json)

```json
{
  "mcpServers": {
    "codeb-deploy": {
      "command": "node",
      "args": ["/Users/admin/new_project/codeb-server/codeb-deploy-system/mcp-server/dist/index.js"],
      "env": {...}
    }
  }
}
```

### 4.2 문제점

| 문제 | 상태 | 설명 |
|------|:----:|------|
| `codeb-deploy-system` 디렉토리 없음 | 🔴 | 삭제됨 또는 이동됨 |
| v6.0 HTTP API와 중복 | 🟡 | 두 시스템이 공존 |
| MCP 명령어가 실제로 작동 안함 | 🔴 | 파일 없음 |

### 4.3 권장 조치

```
현재:
  .mcp.json → codeb-deploy-system (없음) → 실패

권장 (옵션 1 - HTTP API 통합):
  .mcp.json → v6.0/mcp-server → HTTP API

권장 (옵션 2 - MCP 재구축):
  codeb-deploy-system 재구축 → SSH 기반 MCP
```

---

## 5. 정리 권장 사항

### 5.1 즉시 제거 (Dead Code)

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         즉시 제거 대상                                       │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  v6.0 HTTP API Tools:                                                       │
│  ─────────────────────────────────────────────────────────────────────────  │
│  • edge_* (6개) - 미구현 상태                                               │
│  • analytics_* (5개) - 미구현 상태                                          │
│  • deploy_project - deploy와 100% 중복                                      │
│  • slot_promote - promote와 100% 중복                                       │
│  총 13개 Tool 제거                                                          │
│                                                                             │
│  Slash Commands:                                                            │
│  ─────────────────────────────────────────────────────────────────────────  │
│  • /sc:workflow - we:workflow와 중복                                        │
│  • /sc:cleanup - cb-cleanup과 중복                                          │
│  • /sc:estimate - 거의 미사용                                               │
│  • /sc:load - 거의 미사용                                                   │
│  • /sc:index - 거의 미사용                                                  │
│  • /cb-pattern - 거의 미사용                                                │
│  • /we:ssh - Admin 전용, CLI 사용 권장                                      │
│  총 7개 Command 제거                                                        │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 5.2 마이그레이션 완료 후 제거

```
• migrate_* (7개) - v5→v6 완료 후
• env_migrate (1개) - 마이그레이션 완료 후
• codeb-delegate, codeb-new, codeb-optimize 등 레거시 명령어
```

### 5.3 통합 권장

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                          통합 권장                                          │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Before                           After                                     │
│  ─────────────────────────────────────────────────────────────────────────  │
│  team_create + team_list +        team (CRUD 통합)                         │
│  team_get + team_delete +                                                  │
│  team_settings                                                              │
│                                                                             │
│  member_invite + member_remove +  member (CRUD 통합)                       │
│  member_list                                                                │
│                                                                             │
│  domain_setup + domain_verify +   domain (통합)                            │
│  domain_list + domain_delete +                                              │
│  ssl_status                                                                 │
│                                                                             │
│  slot_status + slot_list +        slot (통합)                              │
│  slot_cleanup                                                               │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## 6. 핵심 서비스 플로우

### 6.1 배포 플로우 (실제 사용)

```
개발자 → git push → GitHub Actions → deploy.yml
                                         │
                                         ▼
                              ┌──────────────────┐
                              │  Docker Build    │
                              │  GHCR Push       │
                              └────────┬─────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │  SSH to Server   │
                              │  (appleboy/ssh)  │
                              └────────┬─────────┘
                                       │
                                       ▼
                              ┌──────────────────┐
                              │  podman pull     │
                              │  systemctl       │
                              └──────────────────┘
```

### 6.2 수동 배포 플로우 (we CLI)

```
개발자 → /we:deploy myapp staging
              │
              ▼
      ┌──────────────────┐
      │  MCP codeb-deploy│  ← 현재 작동 안함 (파일 없음)
      └────────┬─────────┘
               │
               ▼
      ┌──────────────────┐
      │  v6.0 HTTP API   │  ← deploy tool 호출
      │  POST /api/tool  │
      └────────┬─────────┘
               │
               ▼
      ┌──────────────────┐
      │  SSH to Server   │
      │  Blue-Green      │
      └──────────────────┘
```

### 6.3 상태 확인 플로우

```
개발자 → /we:health
              │
              ▼
      ┌──────────────────┐
      │  v6.0 HTTP API   │
      │  GET /health     │
      └────────┬─────────┘
               │
               ▼
      ┌──────────────────┐
      │  Server Status   │
      │  Container Info  │
      │  Resource Usage  │
      └──────────────────┘
```

---

## 7. 최종 권장 아키텍처

### 7.1 정리 후 구조

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                     정리 후 CodeB v6.0 구조                                  │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  Slash Commands (/we:* 만 유지, 현재 40개+ → 10개)                          │
│  ─────────────────────────────────────────────────────────────────────────  │
│  핵심: /we:deploy, /we:workflow, /we:health, /we:domain, /we:rollback      │
│  초기화: /we:init, /we:quick, /we:secrets, /we:registry                    │
│  기타: /we:monitor                                                          │
│                                                                             │
│  제거: /cb* (레거시 MCP)                                                    │
│  제외: /sc:* (외부 MCP, CodeB와 무관)                                       │
│                                                                             │
│  HTTP API Tools (18개, 현재 45개 → 60% 감소)                                │
│  ─────────────────────────────────────────────────────────────────────────  │
│  Core: deploy, promote, rollback, slot_status                              │
│  Team: team (CRUD), member (CRUD), token (CRUD)                            │
│  ENV: env_scan, env_restore                                                │
│  Domain: domain_setup, domain_list, domain_delete                          │
│  System: health, logs_stream                                               │
│                                                                             │
│  MCP Server (1개)                                                           │
│  ─────────────────────────────────────────────────────────────────────────  │
│  codeb-deploy → v6.0 HTTP API 프록시                                       │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 7.2 정리 효과

| 항목 | Before | After | 감소율 |
|------|:------:|:-----:|:------:|
| Slash Commands | ~40+ | 15 | 62% |
| HTTP API Tools | 45 | 18 | 60% |
| 코드 복잡도 | 높음 | 낮음 | - |
| 유지보수 부담 | 높음 | 낮음 | - |
| 문서화 필요량 | 많음 | 적음 | 60% |

---

## 8. 실행 계획

### Phase 1: 즉시 정리 (1일)

1. [ ] edge_* 6개 Tool 제거
2. [ ] analytics_* 5개 Tool 제거
3. [ ] 중복 Tool 제거 (deploy_project, slot_promote)
4. [ ] 미사용 Slash Command 제거 (7개)

### Phase 2: MCP 정비 (1일)

1. [ ] codeb-deploy-system 상태 확인
2. [ ] v6.0 HTTP API 기반 MCP 프록시 생성
3. [ ] .mcp.json 업데이트

### Phase 3: 통합 (2일)

1. [ ] team_* → team CRUD 통합
2. [ ] member_* → member CRUD 통합
3. [ ] domain_* → domain 통합
4. [ ] 문서 업데이트

### Phase 4: 테스트 (1일)

1. [ ] 핵심 플로우 테스트
2. [ ] 배포 테스트
3. [ ] 롤백 테스트

---

*문서 버전: 1.0.0 | 최종 업데이트: 2026-01-07*
